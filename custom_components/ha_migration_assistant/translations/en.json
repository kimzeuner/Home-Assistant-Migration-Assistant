{
  "config": {
    "step": {
      "user": {
        "title": "Create migration plan",
        "description": "Enter the old and new entity ID. The assistant scans your Home Assistant configuration, generates reports, and does not modify files.",
        "data": {
          "old_entity_id": "Old entity ID",
          "new_entity_id": "New entity ID"
        }
      }
    },
    "error": {
      "invalid_entity_id": "Enter a valid entity ID like light.kitchen_old."
    },
    "abort": {
      "already_configured": "This migration plan already exists."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Update migration plan",
        "data": {
          "old_entity_id": "Old entity ID",
          "new_entity_id": "New entity ID"
        },
        "description": "Update the entity IDs used for this migration plan."
      }
    }
  }
}
